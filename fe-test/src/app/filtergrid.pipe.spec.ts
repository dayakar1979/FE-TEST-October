import { FiltergridPipe } from './filtergrid.pipe';

describe('FiltergridPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltergridPipe();
    expect(pipe).toBeTruthy();
  });
});
